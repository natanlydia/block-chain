contract Sidekick {
    function makeContact(address hero) external {
        // TODO: trigger the hero's fallback function!
         (bool success, ) = hero.call(abi.encodeWithSignature("nonexistentFunction()"));
        require(success, "Fallback call to hero failed");
    }
}
