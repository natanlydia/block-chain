contract Contract {
    int8 public a = -60;
    int8 public b = 60;
    int16 public difference;

    constructor(){
        int16 temp = int16(a) - int16(b); 
        difference = temp >= 0 ? temp : -temp;
        
    }
}
